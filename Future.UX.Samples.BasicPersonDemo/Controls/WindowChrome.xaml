<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate TargetType="{x:Type Window}"
                     x:Key="WindowTemplate">
        <Grid x:Name="OuterRoot">
            <Grid x:Name="LayoutRoot">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Background="{StaticResource UX.WindowTitleBar.Brush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal">
                        
                    </StackPanel>
                    <TextBlock Grid.Column="1" 
                               Text="{TemplateBinding Title}"
                               FontSize="16"
                               VerticalAlignment="Center"
                               Padding="5" />
                    <StackPanel Grid.Column="2"
                                HorizontalAlignment="Right"
                                WindowChrome.IsHitTestVisibleInChrome="True"
                                Orientation="Horizontal">
                        
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="1">
                    <ContentPresenter />
                </Grid>
            </Grid>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="WindowState"
                     Value="Maximized">
                <Setter TargetName="LayoutRoot"
                        Property="Margin"
                        Value="7" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style TargetType="{x:Type Window}"
           x:Key="WindowStyle">
        <Setter Property="Template"
                Value="{StaticResource WindowTemplate}" />
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome CaptionHeight="30"
                              CornerRadius="5"
                              GlassFrameThickness="0"
                              ResizeBorderThickness="5"
                              UseAeroCaptionButtons="False" />
            </Setter.Value>
        </Setter>
        <Setter Property="Background"
                Value="{StaticResource UX.Background.Brush}" />
        <Setter Property="Foreground"
                Value="{StaticResource UX.Foreground.Brush}" />
    </Style>
    <Style TargetType="{x:Type Window}"
           BasedOn="{StaticResource WindowStyle}" />
</ResourceDictionary>